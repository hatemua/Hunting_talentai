const _siteUrl = process.env.NEXT_PUBLIC_SITE_URL ?? 'https://Hunting.talentai.bid'

// ── Site URLs ──────────────────────────────────────────
export const siteHome     = `${_siteUrl}/`
export const siteOgImage  = `${_siteUrl}/og-card.jpg`
export const siteLogo     = `${_siteUrl}/logo.svg`
export const siteOrgId    = `${_siteUrl}/#org`
export const siteCta      = `${_siteUrl}/#cta`
export const siteLocaleFr = `${_siteUrl}/fr/`

// ── Contact ────────────────────────────────────────────
export const contactEmail     = 'contact@talentai.bid'
export const contactEmailHref = `mailto:${contactEmail}`

// ── Social & booking ───────────────────────────────────
export const linkedinUrl   = 'https://www.linkedin.com/company/talent-ai-labs'
export const linkedinLabel = 'linkedin.com/company/talent-ai-labs'
export const calendlyUrl   = 'https://calendly.com/talent__ai/30min'

// ── External authorities ───────────────────────────────
export const registrucentrasUrl = 'https://www.registrucentras.lt/'
export const vdaiUrl            = 'https://vdai.lrv.lt/'
